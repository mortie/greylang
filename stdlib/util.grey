// 'range' function which takes a min (inclusive) and max (exclusive)
range := {(min, max)
	{
		$iter: {
			i := min;

			next := {
				if(i < max, {
					i = i + 1;
					i - 1
				}, { none })
			};

			{ $next: next }
		}
	}
};

// x percent of y
%of := {(x, y)
	y * (x / 100)
};
